{
  "schemaVersion": 1,
  "id": "io.github.nibra180.clockify",
  "name": "Clockify",
  "version": "1.1.1",
  "author": "nibra180, based on matyssxdxd/clockify",
  "license": "MIT",
  "description": "Start, stop, and watch your Clockify timer from the Omarchy bar.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Clockify",
    "description": "A bar glyph that goes bold while a timer runs, with a panel to start and stop it.",
    "category": "Time",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 60,
      "hideWhenIdle": false
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 15,
        "max": 3600,
        "step": 15,
        "defaultValue": 60,
        "description": "How often the bar re-reads the running timer from Clockify."
      },
      {
        "key": "hideWhenIdle",
        "type": "boolean",
        "label": "Hide when no timer is running",
        "defaultValue": false,
        "description": "The widget still appears while no API key is stored, so you can connect one."
      }
    ]
  }
}
